# OpenAI — used by lib/llm for plain-English file explanations
# (model is pinned in lib/llm/client.ts: gpt-6-luna, reasoning effort medium)
# OPTIONAL: without a key the city works fully; explanations and change
# summaries show a readable disabled message instead.
OPENAI_API_KEY=""
