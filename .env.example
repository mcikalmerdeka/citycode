# OpenCode Go — OpenAI-compatible endpoint (used by lib/llm for summaries)
# Docs: https://opencode.ai/docs/go/
# The openai SDK appends /chat/completions itself, so use the /v1 root.
OPENCODE_BASE_URL="https://opencode.ai/zen/go/v1"
OPENCODE_API_KEY=""
OPENCODE_MODEL="glm-5.3-flash"
